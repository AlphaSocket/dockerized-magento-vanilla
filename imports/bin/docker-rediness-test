#!/bin/sh
#
# Readiness test
#
set -e

CONTAINER_STATUS=$( cat /tmp/container_status )
if [ "$CONTAINER_STATUS" != 'ready' ]; then
    exit 1
fi

EXIT_CODE=1
if [ ! -z "exit 0" ]; then
    sh -c "exit 0"
    EXIT_CODE=$?
fi

exit $EXIT_CODE