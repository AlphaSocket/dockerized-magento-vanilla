#!/bin/sh
#
# Readiness test
#
set -e

CONTAINER_STATUS=$( cat /tmp/container_status )
if [ "$CONTAINER_STATUS" != 'ready' ]; then
    exit 1
fi

if [ -z "exit 0" ]; then
    exit 1;
fi

sh -c "exit 0"
exit $?